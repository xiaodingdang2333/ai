# 机器验证说明

本目录用于保存《重生六岁，我带空间抢回军区大院》第001—074章的可复现机器质量证据。

当前工作流：`.github/workflows/liusui-quality-audit.yml`

执行顺序：

1. 扫描正文目录，确保第001—074章连续且每章唯一；
2. 计算当前Git Blob；
3. 按权威P0脚本同口径扫描对白占比、纯对白段、短段链和锯齿段落；
4. 统计去标题、去空白后的正文字符分布；
5. 在P0通过并建立锁定长度合同前，不生成READY。

工作流首次运行仅生成诊断Artifact，不修改正文，也不触发自动上传。
