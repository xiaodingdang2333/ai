# Workflow Preferences

- Default to action: inspect, edit, generate, or run commands when the request is clear.
- Keep updates short and practical.
- Use the least-token accurate path: avoid broad scans, redundant explanations, and unnecessary web searches.
- For local file transformations, produce the finished file and give the path.
- For media tasks, prefer local deterministic tools when available, such as FFmpeg, PowerShell, browser screenshots, or local scripts.
- For GitHub access in this environment, prefer normal `origin` first, but if HTTPS `git push` hangs while `git ls-remote` works, suspect Git Credential Manager. Clear stale Git/GCM processes, test with `git -c credential.helper= push --dry-run origin main`, and use SSH push (`git push git@github.com:xiaodingdang2333/ai.git main`) when SSH auth is available.
- For Fanqie chapter uploads, avoid repeatedly bouncing between chapter management and draft box pages. Confirm draft state once, then continue from the current page or use targeted checks.
- For this web-novel workspace, formal new or revised chapters should normally be about 3000 Chinese characters unless the user says otherwise. Match the early-chapter style: paragraph lengths should vary, avoid one-sentence-per-paragraph formatting, and preserve a fuller scene texture.
- Novel-writing requirements are mandatory defaults, not optional suggestions. Every time chapters are written, revised, uploaded, or evaluated, enforce them without waiting for the user to repeat them: every chapter the assistant writes must be at least 2500 Chinese characters in all circumstances, with no exceptions; formal chapters should normally target about 3000 Chinese characters; do not satisfy length by mechanically merging short chapters; avoid流水式对话, one-sentence paragraph pacing, dense template phrases, and overly functional/cliche titles; preserve strong hooks, visible conflict, concrete evidence/rules/body cost/public backlash, fuller scene texture, varied paragraph lengths, and chapter-end curiosity. Before upload or handoff, run a local chapter scan/word-count check and inspect for doubled text, title pasted into body, duplicate chapter numbers, weak title-content fit, and suspicious platform draft-list counts.
- 2026-05-27: After future operations, if an insight has reusable value, record it in the shared memory at `F:\ai\memory` using Obsidian-compatible Markdown and the categories in `README.md`.
- 2026-06-07: After solving any new problem, record the reusable lesson or fix pattern in shared memory, and mirror durable updates to `txt/CLAUDE.md` and `txt/GEMINI.md`, so future runs can avoid repeating the same investigation and save tokens.
- 2026-05-28: Whenever shared memory is updated, also mirror the durable change into `F:\ai\txt\CLAUDE.md` and `F:\ai\txt\GEMINI.md`, so Claude/Gemini can load the same preferences by reading those files.
- 2026-05-28: Keep per-novel concrete details inside each novel folder, not shared memory. Shared memory should store cross-cutting writing/work preferences and reusable workflows only.
- 2026-07-09: 旧“网页自定义GPT + `services/novel-actions` 服务器Action + 服务器保存正文/上传”的小说生产流程已废弃，之后不再用于写作、修订、QA、上传或长任务执行。所有涉及`openapi-gpt.json`、4操作RPC、`runNovelWorkflow`、`runFanqieWorkflow`、`getNovelJob`、`saveNovelAssets`、`next_action.action/payload_schema`、网页Action检查点的旧规则只作为历史事故记录保留，不得作为当前执行依据。当前网页版小说生产权威流程是`/home/admin/chatgpt-novel-production-system`最新`main`中的2.2-LTS Git仓库工作流；服务器后续只能围绕Git仓库产物、current-blob P0、handoff snapshot和可审计状态重新设计能力对齐，不能复活旧Action流程。
- 2026-06-03: When the user asks to write, revise, package, or evaluate fiction for 七猫投稿/七猫内投, first read `F:\ai\memory\qimao-submission-rules.md` and apply its ranking-derived opening rules.
- 2026-06-03: When starting a new 七猫投稿/七猫内投 novel project, default to producing at least 20,000 Chinese characters of trial-submission draft unless the user says otherwise.
- 2026-07-02: 【已废弃：旧自定义GPT Action流程】新书封面曾要求自定义GPT保存提示词、用户手动生成后上传、服务器保存PNG 600×800；该流程不再作为当前小说生产主线。
- 2026-07-02: 【已废弃：旧自定义GPT Action流程】网页版自定义GPT首次3章、后续询问章数/上传模式、网页Action逐章上传验收等规则不再作为当前执行依据。
- 2026-07-02: 【已废弃：旧自定义GPT Action流程】网页自定义GPT提交后台任务后用`getNovelJob(wait_seconds=35)`持续轮询到终态的规则不再作为当前执行依据。
- 2026-07-02: 【已废弃：旧自定义GPT Action流程】服务器终止重复queued/running上传任务的规则只作为旧流程事故记录，不再作为当前写作主线要求。
- 2026-07-02: 【已废弃：旧自定义GPT Action流程】网页GPT调用`getFanqieDraftStatus`判断草稿状态的规则只作为旧流程事故记录。
- 2026-07-01: 【已废弃：旧自定义GPT Action流程】自定义GPT新书流程用`createNovelProject`按书名/账号幂等恢复的规则不再作为当前执行依据。
- 2026-06-18: For new Fanqie quick-transmigration novels, reference the user's latest downloaded `txt/排行榜/番茄排行榜/` quick-transmigration ranking samples for title/intro/chapter format, wording, and rhythm. Favor direct list-style tags, strong contrast premises such as "they want X, I do Y", quick unit labels, lighter oral wording, and immediate anti-plot behavior, while still keeping original concepts rather than copying sample plots or phrasing.
- 2026-06-24: For all future quick-transmigration/快穿 novels, default chapter titles to the user's established Fanqie format: `第XXX章 小世界身份/单元名N`, where `N` is the within-world sequence number, e.g. `第001章 豪门真千金讨债1`, `第036章 古代退婚嫡女1`, or `第001章 豪门病弱真千金1`. Do not default to standalone literary/object-hook chapter titles like `雨夜门外` or `三个月病危单` for 快穿 books unless the user explicitly asks to change style.
- 2026-06-24: When giving Fanqie/Tomato book-creation info, generate the tag/category fields according to the user's screenshot of Fanqie creation options and requirements, not as free-form comma-separated tags. Use `/home/admin/ai/memory/fanqie-book-creation-tags.md` for the captured rules/options when available. If the screenshot/options are not available in the current context, say so and ask the user to re-upload or paste the options before finalizing tag info.
- 2026-06-18: For future Fanqie/Tomato novel publishing, default to the stable backend API publishing workflow from `/home/admin/ai/scripts/fanqie-api-publish.js` and `/home/admin/ai/scripts/fanqie-api-daily-publish.sh`, not Chrome/CDP click automation. Publish until the platform returns the daily word-limit message, verify the expected author account, and send PushPlus for failures; for scheduled draft exhaustion, notify only when a book's draft box is fully published.
- 2026-06-18: Fanqie scheduled publishing notifications should default to PushPlus only. Do not send the old "remaining draft words below 20,000" alert; instead notify when a book's draft box has been fully published and `final_draft_total` is 0.
- 2026-06-27: Fanqie/Tomato publishing is manual by default. When asked to write or continue a novel, AI completes writing and mandatory local QA, automatically uploads only the new chapters to the verified account and book draft box, and verifies chapter number, title, and platform word count. The user performs the final publish action. Do not create scheduled publishing jobs or expose automatic publish controls unless the user explicitly reverses this policy. Full workflow and incident history: `/home/admin/ai/memory/fanqie-manual-publishing-workflow.md`.
- 2026-06-27: For every future new Fanqie/Tomato book, first run the `fanqie-novel-ideation` funnel: analyze only functional DNA from relevant local ranking samples, generate exactly 12 structurally distinct ideas, score them, retain 3, and wait for the user's explicit selection. Do not create a novel project or write chapters before selection. After selection, create the project and write exactly 3 trial chapters for approval; bulk writing/upload needs later approval. Existing novels are exempt.
- 2026-07-03: All future novel work through server Codex or the current web Git workflow must enforce the shared eight-part originality gate before creation, writing, QA, or upload: ban first-thought defaults; structurally separate the 12 candidates; add external entropy constraints; compare cross-book creative fingerprints; run the costume-swap test; require scene causality (goal, obstacle, choice, cost, state change); conduct a separate adversarial AI-pattern review; and keep roughly 70% genre familiarity / 30% structural novelty. Missing or failed evidence blocks progression. This reduces but cannot guarantee elimination of AI-detection risk. Canonical rules: `/home/admin/ai/codex/skills/fanqie-novel-ideation/references/originality-gate.md`.
- 2026-07-03: New-book ranking samples must not default to Fanqie. Male-oriented projects use the strict fallback chain `起点 → 飞卢 → 七猫 → 番茄`; female-oriented projects use `晋江 → 七猫 → 番茄`. Start with the first platform and move forward only when the current platform fails or yields fewer than three usable books after its batch; never skip ahead. SoNovel may fetch a mirror copy, but identity and ranking selection must be anchored to the current official platform. This applies equally to Codex and the current web Git workflow.
- 2026-07-03: Novel chapters use a mandatory two-phase save flow. Generated or revised text is first persisted as recoverable temporary drafts, then checked for length, contamination, duplication, paragraph rhythm, dialogue density, AI-template language, cross-book similarity, scene causality, continuity, and originality. Failed drafts remain editable and never enter formal `正文/` or upload queues. The first three chapters require explicit user approval before promotion; later batches follow their configured `auto` or `review` mode. Proposed tracking state is staged and applied atomically only when chapters are promoted.
- 2026-07-04: 【已废弃：旧自定义GPT Action流程】网页GPT长篇连续写作逐章提交服务器原子检查点、以服务器`revision`和`next_action`恢复的规则不再作为当前执行依据。
- 2026-07-04: 【已废弃：旧自定义GPT Action流程】网页GPT通过服务器`book_id`模糊查书、旧稿原子暂存修订、重置上传状态等规则不再作为当前执行依据。
- 2026-07-04: 【已废弃：旧自定义GPT Action流程】网页GPT持续轮询番茄长批量上传任务的规则不再作为当前执行依据。
- 2026-07-04: 【已废弃：旧自定义GPT Action流程】小说项目检索覆盖`novel-actions`数据库并自动导入旧书的规则只作为旧流程事故记录。
- 2026-07-05: 【已废弃：旧自定义GPT Action流程】网页自定义GPT导入`services/novel-actions`提供的4操作`openapi-gpt.json`并通过`runNovelWorkflow`、`runFanqieWorkflow`、`getNovelJob`、`saveNovelAssets`完成写作和上传的规则已废弃；不得再作为当前小说生产主线。
- 2026-06-27: For Fanqie ranking-study downloads, use the official Fanqie ranking to choose books, then use the local SoNovel packet workflow. Do not reject a result merely because the mirror author is `佚名` or its chapter count trails the official site; verify the synopsis or first few chapter titles. If no exact result exists or identity cannot be confirmed, skip it and continue with the next ranked book. Analyze selected chapters only instead of loading a whole novel into model context.
- 2026-06-06: For Fanqie/Tomato uploads with multiple author accounts, keep separate browser profiles/cookie caches: `account-a` is for the `西大水怪` account; `account-b` is for the `桃枝醒醒` account. Do not use one account's profile for the other account.
- 2026-06-07: Fanqie/Tomato account map also includes `account-c` for `泡芙软呼呼`; publishing under this account defaults to declaring no AI use.
- 2026-06-06: To refresh a Fanqie/Tomato QR login for the existing public QR page, use the already-running CDP browser for the target account profile, click the visible `立即登录` button by DOM `button` text, then click the `扫码登录` element by exact text across all DOM nodes, extract the QR from the large `data:image/png;base64,...` image, and overwrite `/home/admin/ai/output/qr-login/current.png`. The public page is `http://8.212.144.72:8090/`; avoid starting a duplicate HTTP server.
- 2026-06-06: When creating or installing Codex skills, put user-created/downloaded skills in the workspace skill directory `F:\ai\codex\skills` / `/home/admin/ai/codex/skills` by default. If Codex discovery requires `~/.codex/skills`, create a symlink there instead of keeping the canonical copy outside the AI workspace.
- 2026-06-11: For People's Daily Online / Renminwang investment notifications, before recommending an operation always refresh the latest quote and rerun a vibetrading-style review of current price action and portfolio context; if the recommendation changes, notify using the revised recommendation.
- 2026-06-11: For investment operation recommendations and Renminwang notifications, include detailed reasons rather than only the action: latest price/market context, portfolio impact, trigger rule, why the recommendation changed or stayed the same, and the invalidation/next-action conditions.
- 2026-06-11: Any future fund or stock operation notification must include a web feedback link where the user can record what they actually did. If the user does not submit feedback, assume no operation. Submitted feedback should update the relevant local record when a dedicated state exists, otherwise append to the general trade feedback log.
- 2026-06-11: The user often provides task inputs through the web UI upload flow, including screenshots, files, and page captures. Treat uploaded web content as the primary task context when present. If the current session/model context cannot access the uploaded content, state that explicitly and ask the user to re-upload or paste the key details instead of assuming the user did not provide it.
- 2026-06-13: For unattended investment monitors, avoid calling Codex from cron/systemd because it consumes Codex quota and is less stable for background jobs. Prefer script-first hard rules plus an OpenAI-compatible API provider for optional vibetrading-style review. The current preferred pattern is DeepSeek model routing: normal key-node reviews use `deepseek-chat`; hard action signals, extreme moves, or prices very close to action levels use `deepseek-reasoner`; never store API keys in shared memory.
- 2026-06-30: When the user asks to sync the AI workspace to the remote repository, default to a migration-grade sync: include worthwhile workspace content plus Codex skills, installed plugins, portable configuration, and historical task/session state so another server can substantially reproduce the current Codex environment after cloning. Because the remote repository may be public, encrypt sensitive history/state with a separately stored recovery passphrase; never commit authentication files, API tokens, browser cookies/profiles, live service credentials, locks, logs, downloaded binaries/caches, or other reproducible runtime artifacts. Provide and verify a restore script as part of the sync.
